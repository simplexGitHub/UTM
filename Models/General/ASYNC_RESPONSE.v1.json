{
  "title": "ASYNC_RESPONSE",
  "type": "object",
  "description": "The basic response for requests",
  "properties": {
    "success": {
      "type": "boolean",
      "description": "If true then the request succeeded"
    },
    "description": {
      "type": "string",
      "description": "A string to describe the response.\nIf success = false then a description if the cause can be added.\nIf no description - blank"
    },
    "errorMessage": {
      "type": "string",
      "description": "A pre defined error enums in the level of the general response (could be more specific requests levels of errors)\n",
      "enum": [
        "GeneralError",
        "JsonNotValid"
      ]
    },
    "data": {
      "type": "object"
    }
  },
  "required": [
    "success",
    "description"
  ]
}